jackson:
  serializationInclusion: ALWAYS

consul:
  client:
    registration:
      enabled: true

otel:
  exporter:
    otlp:
      endpoint: http://localhost:4317
  traces:
    exporter: otlp
  exclusions:
    - /v1/.*