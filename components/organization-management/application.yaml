#include postgres-keycloak, logback-template, bootstrap, keycloak, kafka, kubernetes

micronaut:
  application:
    name: organization-management

flyway:
  datasources:
    keycloak:
      enabled: false
      baseline-on-migrate: true
      locations:
        - classpath:/db/keycloak/migration