#include postgres-default, logback-template, bootstrap, kafka, kubernetes

micronaut:
  application:
    name: library

flyway:
  datasources:
    default:
      enabled: false
      locations:
        - classpath:/db/library/migration
